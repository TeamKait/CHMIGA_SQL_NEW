>СТРУКТУРА ТАБЛИЦЫ
```sql
create table m1001(
	id int primary key,
    f_name varchar(40),
    l_name varchar(40),
    date1 date,
    product1 varchar(20),
    product2 varchar(20)
);
```

>ЗАПОЛНЕНИЕ ТАБЛИЦЫ
```sql
-
```